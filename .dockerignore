# Dependencies (installed fresh inside the image)
node_modules
ui/node_modules

# UI is built and served by its own image
ui

# VCS
.git
.gitignore
.gitattributes

# Docs
*.md
LICENSE

# OS / editors
.DS_Store
.idea
.vscode

# Logs / temp
*.log
logs
/tmp
tmp

# Env / secrets
.env
.env.*

# Test / coverage
coverage
.nyc_output

# Claude config
.claude

# Docker build files (not needed inside the image)
Dockerfile
docker-compose.yml
.dockerignore
